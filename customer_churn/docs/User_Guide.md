# User Guide

1. Install dependencies from the repository root.
2. Review or update `config.yaml`.
3. Run `python src/train.py` to regenerate the model and outputs.
4. Start the app with `streamlit run app/streamlit_app.py`.
